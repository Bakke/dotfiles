# Directory and file navigation/editing
alias md='mkdir -p'
alias rd='rmdir'
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias ll='ls -li'
alias la='ls -la'
alias lsd='ls -l | grep "^d"'
alias fs="sizeof"
alias pwdcp='pwd | pbcopy'
alias -g vr="| nvim -R -"
alias c='pygmentize -O style=monokai -f console256 -g'

# Find
alias fd='find . -type d -name'
alias ff='find . -type f -name'

# Git
alias gs='git status'
alias gss='git status -s'
alias gcl='git clone'
alias ga='git add'
alias gall='git add -A'
alias gm="git merge"
alias gp='git push'
alias gpo='git push origin'
alias gpu='git push --set-upstream'
alias gl='git pull'
alias gf='git fetch && git rebase'
alias gco='git checkout'
alias gcb='git checkout -b'
alias gls="git log --pretty=format:'%s'"
alias gll='git log --graph --pretty=oneline --abbrev-commit'
alias gg="git log --graph --pretty=format:'%C(bold)%h%Creset%C(yellow)%d%Creset %s %C(yellow)%an %C(cyan)%cr%Creset' --abbrev-commit --date=relative"
alias ggs="gg --stat"
alias gsl="git shortlog -sn"
alias gw="git whatchanged"
alias gd='git diff | nvim -R -'
alias gt="git tag"
alias gta="git tag -a"

# Tmux
alias tm='tmux new-session -A -s main'
alias txl='tmux ls'
alias txn='tmux new-session -A -s'
alias txa='tmux a -t'
alias txk='tmux kill-session -t'

# Misc
alias q='exit'
alias reload="source ~/.zshrc"
alias artisan='php artisan'
# alias artisanclear='artisan cache:clear && artisan clear-compiled && artisan optimize'
alias flush='dscacheutil -flushcache'
alias excuse='echo "$(curl --silent https://api.githunt.io/programmingexcuses)"'
alias idriftweb-packages-update='curl --silent https://packages.idriftweb.no/update.php'

# Fasd
alias a='fasd -a'        # any
alias s='fasd -si'       # show / search / select
alias d='fasd -d'        # directory
alias f='fasd -f'        # file
alias sd='fasd -sid'     # interactive directory selection
alias sf='fasd -sif'     # interactive file selection
alias z='fasd_cd -d'     # cd, same functionality as j in autojump
alias zz='fasd_cd -d -i' # cd with interactive selection
alias vv='a -e vim'       # Open any file/dir with vim
